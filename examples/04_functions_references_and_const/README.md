# Block 04 experiments

Exactly two experiments accompany the lecture:

1. `01_course_inventory_refactored` — the block-03 inventory rewritten as small functions while preserving its command line and report format.
2. `02_function_contract_lab` — runnable value/reference/return/overload/function-pointer cases, expected compile failures, and an isolated dangling-return sanitizer case.

Each directory contains its own build and PASS instructions.
