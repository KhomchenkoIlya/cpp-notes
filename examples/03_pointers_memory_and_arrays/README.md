# Block 03: pointers, memory, and arrays

The block contains exactly two experiments:

1. `01_course_inventory` — a read-only inventory of all 15 course blocks.
2. `02_array_vs_vector_lab` — safe storage demonstrations and isolated sanitizer cases.

Each directory is an independent CMake project. Build out of source as shown in its README.
