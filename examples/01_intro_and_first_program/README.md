# Практика к лекции 01

1. `01_compilation_pipeline/` — вручную показывает preprocessing, assembly, object file и linking.
2. `02_basic_abstractions/` — использует `std::string`, `std::vector`, `std::map` и минимальный CMake.

Сначала рекомендуется пройти pipeline, а затем пользоваться первой удобной абстракцией над сборкой.
