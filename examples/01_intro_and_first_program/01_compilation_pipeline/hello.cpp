#include <iostream>

int main() {
    // std::cout — стандартный поток вывода.
    // Префикс std:: явно показывает, откуда пришло имя cout.
    std::cout << "Hello from the compilation pipeline!\n";

    // Нулевой код завершения сообщает об успешном выполнении.
    return 0;
}
