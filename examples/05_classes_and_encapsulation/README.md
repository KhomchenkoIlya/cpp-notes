# Block 05 practice

Exactly two experiments support the lecture:

1. `01_course_inventory_classes` — the block-04 inventory refactored around a small encapsulated `CourseBlock` domain type while preserving its CLI and report.
2. `02_encapsulation_break_lab` — positive cases plus reproducible compile failures for private access and mutation through a const object.

Both projects use C++17, target-based CMake, compiler-specific warning flags, out-of-source builds, and CTest.
