# constexpr_and_concepts_lab

Требует C++20.

Показывает `constexpr`, `consteval`, `constinit`, standard/custom Concepts,
`requires`, constrained overload selection и два real compile-failure cases.

Запуск:

```sh
./run_checks.sh
```

Safe path дополнительно проходит ASan/UBSan.
